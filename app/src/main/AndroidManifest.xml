<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ssso_knrdist">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/saibaba_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/saibaba_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Wings.NotificationActivity"></activity>
        <activity android:name=".Wings.WingsActivity" />
        <activity android:name=".Samithies.SamithiesActivity" />
        <activity android:name=".Activties.RegisterActivity" />
        <activity android:name=".Activties.BottomPageActivity" />
        <activity android:name=".Activties.LoginActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name=".MyMessagingService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>